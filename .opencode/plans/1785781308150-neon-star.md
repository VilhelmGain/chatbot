# Fix Docker Deployment

## Changes Made (local)
**File:** `docker-compose.yml` (line 45)
- Changed Redis port mapping: `"6379:6379"` → `"6380:6379"` (port 6379 was in use on server)

## Server Status
- DNS resolution: ✓ Working (tested with `docker run --rm alpine nslookup registry.npmjs.org`)
- Docker daemon.json: Empty (using defaults, no changes needed)

## Deploy on Server
```bash
docker compose down
docker compose up -d
```

## Verification
- `docker compose ps` — all 3 containers (app, postgres, redis) should be running/healthy
- `docker compose logs app` — no errors
